


#spring.security.enable-csrf=false
spring.security.basic.enabled=true
spring.security.user.name=henny
spring.security.user.password=1234

#server.port=8081

# ;integratedSecurity=true;encrypt=false; <- configuracion para conexion con 'windows authentication..."
spring.datasource.url=jdbc:sqlserver://Henny\\mingi:1433;databaseName=rental;encrypt=false;
spring.datasource.username=sa
spring.datasource.password=feferrefe2020

spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.org.hibernate.dialect.SQLServer2008Dialect

# = Logging

# Level for loggers on classes inside the root package "application" (and its
# sub-packages).
# Available levels are: TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF
logging.level.application = DEBUG

# Fine-tuning a specific logger (for a single class)
# logging.level.application.controllers.HomeController = TRACE

# Specify the level for spring boot and hibernate's loggers
logging.level.org.springframework.web = DEBUG
# logging.level.org.hibernate = ERROR

# Log file location (in addition to the console)
#logging.file = F:\\spring-boot-logging.log